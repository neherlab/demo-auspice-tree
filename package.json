{
  "name": "demo-auspice-tree",
  "version": "0.1.0",
  "description": "Demonstrates how to use Auspice tree component in React app",
  "license": "MIT",
  "private": true,
  "scripts": {
    "dev": "yarn install && next dev --hostname 0.0.0.0 --port 3000",
    "prod:build": "yarn install && next build && next export"
  },
  "dependencies": {
    "next": "9.5.3",
    "react": "16.13.1",
    "react-dom": "16.13.1"
  }
}
